/* abstract class ProfileUseCases {
  Future<User> onEditUserName(String name);
  Future<User> onEditUserEmail(String email);

  /// For User Profile
  Future<User> onEditUserMobileNo(String mobile);
  Future<User> editUserProfileImage(String image);
  Future<User> editUserRole(String role);

  Future<User> UserNotifications(String notification);
}
 */